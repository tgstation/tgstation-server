{
  "private": true,
  "dependencies": {
    "react-dom": "^16.5.2",
    "tgstation-server-control-panel": "0.1.5"
  },
  "scripts": {
    "build": "rm -rf build && react-scripts-ts build && cp -r node_modules/tgstation-server-control-panel/build/public/* build/ && cp ../../../build/tgs.ico build/favicon.ico && rm -rf ../wwwroot && mv build ../wwwroot"
  },
  "devDependencies": {
    "@types/node": "^10.12.0",
    "@types/react-dom": "^16.0.9",
    "react-scripts-ts": "^3.1.0",
    "typescript": "^3.1.3"
  }
}
