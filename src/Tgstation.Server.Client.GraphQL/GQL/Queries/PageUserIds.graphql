query PageUserIds($first: Int, $after: String) {
  swarm {
    users {
      queryable(first: $first, after: $after) {
        pageInfo {
          endCursor
          hasNextPage
        }
        totalCount
        nodes {
          id
        }
      }
    }
  }
}
